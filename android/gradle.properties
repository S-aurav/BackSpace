org.gradle.jvmargs=-Xmx4096M -Dhttps.protocols=TLSv1.2 -Djdk.tls.client.protocols=TLSv1.2
android.useAndroidX=true
android.enableJetifier=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Enable BuildConfig generation globally (required by AGP 8.x for plugins like cloud_firestore)
android.buildFeatures.buildConfig=true

# Fix Kotlin Windows cross-drive bug (C:\ pub cache vs F:\ workspace project)
kotlin.incremental=false
kotlin.incremental.android=false
kotlin.incremental.jvm=false

